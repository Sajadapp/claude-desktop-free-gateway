# Secrets — never commit real keys (samples only in .env.example)
.env

# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Runtime / local data
sandbox/
captures/
*.log

# Local dev/test scripts (may contain private machine paths) - never publish
test_*.py
